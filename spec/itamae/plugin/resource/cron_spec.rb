require 'spec_helper'

describe Itamae::Plugin::Resource::Cron do
  it 'has a version number' do
    expect(Itamae::Plugin::Resource::Cron::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
